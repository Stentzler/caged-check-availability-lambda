[project]
name = "caged-check-availability-lambda"
version = "0.1.0"
description = "AWS Lambda that checks Novo CAGED file availability on the public FTP server."
requires-python = ">=3.14"
dependencies = [
    "serverless-toolkit",
]

[tool.uv.sources]
serverless-toolkit = { git = "https://github.com/Stentzler/caged-serverless-toolkit.git", tag = "v0.1.0" }

[dependency-groups]
dev = [
    "pre-commit>=4.0.0",
    "debugpy>=1.8.0",
    "pytest>=8.0.0",
    "ruff>=0.11.0",
]

[tool.ruff]
extend = "./config/ruff.toml"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]